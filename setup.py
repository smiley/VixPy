#!/usr/bin/env python

from setuptools import setup

setup(name='vixpy',
      version='1.0',
      description='A Pythonic implementation of the VMware VIX API.',
      author='@yzlin',
      packages=['vixpy'],
      install_requires=[]
      )
